<style>
  .search-btn{
    background-color: rgb(223, 50, 85);
    color: white;
    border-radius: 25px;
    padding: 0 0.8rem 0 0.8rem;
  }
  .search-btn:hover{
    background-color: rgb(223, 50, 85);
    color: white;
  }
  .search-btn i{
    display: inline;
    margin-right: 0.5rem;
  }
  .fa-compass{
    margin-left: 1rem;
  }
  .search-inp{
    border-radius: 25px;

  }

</style>

<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom sticky-top">
    <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/listings">Explore</a>
      </div>
      <div class="navbar-nav ms-auto">
        <form class="d-flex" role="search">
          <input class="form-control me-2 search-inp" type="search" placeholder="Search destinations">
          <button class="btn search-btn" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
        </form>
      </div>

      <div class="navbar-nav ms-auto">
        <a class="nav-item nav-link" href="/listings/new">New Listing</a>
        <% if(!currUser){ %>
          <a class="nav-item nav-link" href="/users/signup"><b>Signup</b></a>
          <a class="nav-item nav-link" href="/users/login"><b>Login</b></a>
        <% } %>
        <!--  if user not exists then shows the signup and login -->
          <!-- if we directly execute this , this will give error, we should first take 
           req.user as a variable in the locals in app.js and then execute. -->
        <% if(currUser){ %>
          <a class="nav-item nav-link" href="/users/logout"><b>Logout</b></a>
        <% } %>
        <!-- if user exists then shows logout -->
      </div>
    </div>

</nav>